import 'package:flutter/material.dart';

class ScreensHeaderBar extends StatelessWidget {
  const ScreensHeaderBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
